export enum Message {
    EMPTY = "",
    VIDEO_SKIPPED = "Skipped to next video in queue!",
    VIDEO_ADDED = "Added song to queue!",
}

export enum Tab {
    SEARCH = "search",
    QUEUE = "queue",
    HISTORY = "history",
}

export enum KaraokeTerm {
    JAPAN = "カラオケ",
    KOREA = "가라오케",
    ENGLISH = "karaoke",
    CHINESE = "卡拉OK",
}

export enum Language {
    JAPAN = "japan",
    KOREA = "korea",
    ENGLISH = "english",
    CHINESE = "chinese",
}