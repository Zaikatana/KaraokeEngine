export enum Message {
    EMPTY = "",
    VIDEO_SKIPPED = "Skipped to next video in queue!",
    VIDEO_ADDED = "Added song to queue!",
}

export enum Tab {
    SEARCH = "search",
    QUEUE = "queue",
    HISTORY = "history",
}
